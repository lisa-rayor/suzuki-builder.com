name: suzuki-builder
services:
  web:
    image: php:8.4-apache
    ports:
      - "127.0.0.1:8081:80"
    volumes:
      - .:/var/www/html:ro
      - ./docker/apache.conf:/etc/apache2/conf-enabled/site.conf:ro
